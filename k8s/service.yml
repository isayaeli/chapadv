apiVersion: v1
kind: Service
metadata:
  name: chapadv-service
spec:
  selector:
    app: chapadv-app
  ports:
  - port: 8000
    targetPort: 8000
  type: LoadBalancer
---
apiVersion: v1
kind: Service
metadata:
  name: db #this is the service name for Postgres and act as hostname in the DB configuration
spec:
  selector:
    app: postgres
  ports:
  - port: 5432
    targetPort: 5432